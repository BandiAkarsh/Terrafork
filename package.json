{
  "name": "forkzero-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter \"!./services/scraper\" dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "docker:up": "docker-compose up"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}